**ChatFlow** is a small toy project that explores how linear chat conversations can be visualized as branching flows.

## Quick start

Requirements: Node.js (16+) and npm.

```bash
git clone https://github.com/CatheadOwl/ChatFlow.git
cd ChatFlow
npm install
npm run dev
```

Open the local Vite URL shown in the terminal.